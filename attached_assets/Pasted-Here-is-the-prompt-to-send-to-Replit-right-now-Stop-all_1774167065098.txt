Here is the prompt to send to Replit right now:

Stop all feature work. We are shifting focus to design and architecture evaluation in parallel with building real mockups.
What I need from you right now is two things running in parallel:
Task 1 — Design audit of what exists:
Go through every template file and CSS file and give me an honest evaluation:

What does the current dashboard actually look like on a 375px mobile screen right now — describe it honestly, does it break, overflow, or look unusable
What is the current CSS framework — is it custom CSS, Bootstrap, Tailwind, or mixed
What font is currently loaded and is IBM Plex Sans Arabic already imported
Are there any existing 3D effects, glassmorphism, or animations already in the CSS or is it completely flat
What is the current color palette — exact hex values for the main background, card background, accent color, and text color
How many separate CSS files exist and are they organized or scattered
Does the current layout use flexbox, grid, or neither
Is there any RTL support currently or is everything hardcoded left-to-right

Task 2 — Start building the new design system:
Do not wait for the audit to finish. Start building the new design system simultaneously.
Build a single design_system.html file that contains everything — no external dependencies except Google Fonts. This file is the reference for every screen we build after it.
The design language is:
Color palette:

Background: #0a0a0f (near black with slight blue tint)
Surface: #0f1117 (cards and panels)
Glass: rgba(255, 255, 255, 0.04) (glassmorphism fill)
Glass border: rgba(255, 255, 255, 0.08)
Primary accent: #6366f1 (electric indigo)
Secondary accent: #22d3ee (cyan)
Success: #10b981
Warning: #f59e0b
Danger: #ef4444
Text primary: #f1f5f9
Text secondary: #94a3b8
Text muted: #475569

Typography:

Display headings: Space Grotesk (Latin) — weight 700
Body and UI: Inter (Latin) — weight 400 and 500
Arabic text: IBM Plex Sans Arabic — weight 400 and 600
Base size: 16px, scale using clamp() for fluid sizing

The 3D glass card component:
Every card in the entire app uses this exact style:

Background: rgba(255, 255, 255, 0.04)
Border: 1px solid rgba(255, 255, 255, 0.08)
Border radius: 16px
Backdrop filter: blur(20px) saturate(180%)
Box shadow: 0 8px 32px rgba(0, 0, 0, 0.4), inset 0 1px 0 rgba(255, 255, 255, 0.08)
On hover: transform: translateY(-2px) scale(1.01), box shadow deepens
Transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1)

3D depth system:

Use CSS perspective(1000px) on container elements
Cards tilt subtly on mouse move using JavaScript — maximum 5 degrees on any axis
Depth layers: background at z-index 0, cards at z-index 10, modals at z-index 100
Floating orbs in background — 3 large blurred circles in accent colors at 10% opacity that move slowly on scroll using parallax

Animation system:

Page load: elements fade in with translateY(20px) to translateY(0) staggered by 100ms
Skeleton loading: shimmer animation using linear-gradient sweep
Number counters: count up from 0 when they enter the viewport
Success states: subtle pulse in green
All transitions: cubic-bezier(0.4, 0, 0.2, 1) — never linear, never ease

RTL system:

The html tag switches between dir="ltr" and dir="rtl" based on user language preference
Use ONLY logical CSS properties: margin-inline-start, padding-inline-end, border-inline-start, inset-inline-start
Never use margin-left, margin-right, padding-left, padding-right anywhere in the codebase
Text always uses text-align: start, never text-align: left or right
Icons that indicate direction (arrows, chevrons) flip automatically using transform: scaleX(-1) in RTL mode

The design system file must contain these components rendered on screen:

The glass card in 3 sizes — small (stat card), medium (content card), large (full panel)
The primary button with gradient background (indigo to cyan), hover lift effect, and loading spinner state
The secondary button — ghost style with glass border
The input field — dark fill, glass border, focus glow in accent color, with an Arabic placeholder example
A navigation sidebar showing 5 items with icons, active state highlighted with accent color and a left border glow
A bottom tab bar — the mobile equivalent of the sidebar, 5 icons, active state
A stat card showing a number, label, and percentage change with up/down indicator
A badge system — 4 variants: success, warning, danger, info
A skeleton loading card
A notification toast — top right, glass style, with an icon and dismiss button
The 3D tilt effect working on hover — mouse movement tilts the card in real 3D space
The floating background orbs — visible behind all components
A progress bar with gradient fill and animated shimmer
A toggle switch — Arabic label on the right side in LTR, left side in RTL

Deliver:

The design_system.html file as a single self-contained file with zero external dependencies except Google Fonts CDN
The design audit answers as a separate message

Do not start building any actual app screens until I approve the design system. Everything else depends on this file being right first.