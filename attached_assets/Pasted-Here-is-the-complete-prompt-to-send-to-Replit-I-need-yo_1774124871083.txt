Here is the complete prompt to send to Replit:

I need you to do a full analysis of the current codebase and create a detailed plan to transform this from a desktop/developer tool into a proper web application. Before writing any code, just analyse and plan.
Context you need to understand first:
This tool will be deployed on Oracle Cloud Free Tier (or Hetzner VPS). It is a lifetime purchase product sold on Gumroad at $49. Target users are non-technical Arabic-speaking content creators who will access it entirely from a browser on their phone or desktop. They will never touch a terminal. Users bring their own Gemini API key. The tool manages their Facebook and Instagram pages automatically.
Part 1 — Analyse what we currently have:
Go through every file and tell me honestly:

What is the current architecture and why it does not work as a web app for non-technical users
Which files are the core engine that stays untouched (publisher, scheduler, scraper, ai_generator, ban_detector, rate_limiter)
Which files are dead code, duplicates, or developer utilities that get deleted
What the current dashboard_app.py does well and what needs to be completely replaced
What the current database schema looks like and what needs to be added for multi-user support

Part 2 — Create the transformation plan:
Tell me exactly what needs to be built from scratch:
User system:

Registration and login with email and password
Each user has their own isolated data in Supabase using Row Level Security
Each user enters their own Gemini API key stored encrypted in their account
Facebook and Instagram OAuth connection per user
Telegram bot connection per user for mobile notifications and approvals

Web application structure:

Proper Flask application using Blueprints, not one monolithic 2200-line file
Gunicorn + Nginx for production deployment
One clean folder structure — no more 80 files at root level
Environment config that works on Oracle Cloud and Hetzner VPS

Part 3 — The new design system:
The current design is a basic desktop dashboard. The new design must be:

3D modern design — using Three.js or CSS 3D transforms for depth and dimension
Dark mode as default with a deep navy and electric blue color palette
Glassmorphism cards — frosted glass effect for all panels and cards
Smooth animations on all transitions and interactions
Mobile-first — every single screen must work perfectly on a phone screen first, then scale up to desktop
Arabic RTL support built into the layout from the start, not added as an afterthought
The feel should be closer to a premium SaaS tool like Linear or Vercel, not a basic admin panel

Part 4 — The new user journey:
Map out exactly what a new user sees from the moment they arrive at the URL for the first time:

Landing page with demo video and buy button linking to Gumroad
After purchase they get an activation code
They register with email, password, and activation code
Simple 4-step onboarding wizard: connect Facebook → connect Instagram → enter Gemini API key → connect Telegram for mobile notifications
First content is generated automatically within 5 minutes of finishing onboarding
Dashboard shows live status of their pages, scheduled posts, and what is working

Part 5 — Telegram bot plan:
One single Telegram bot running on the server for all users. Each user connects by sending a unique code to the bot. Through Telegram they can:

Receive daily summary every morning
Get notified when a post is published
Approve or reject a post before it goes live with a simple Yes/No button
See today's best performing post
Pause or resume automation with one tap

What I want from you right now:
Do not write any code yet. Analyse the current codebase completely, then give me:

A honest assessment of what stays, what gets deleted, what gets rebuilt
The new folder and file structure as a clean tree
The technology choices for the new frontend design (Three.js, CSS glassmorphism, etc.)
The database migration plan — what tables need to be added or changed
An estimated timeline for the full transformation broken into phases
Any risks or problems you see that we should discuss before starting

Take your time with this. This analysis will drive everything we build next.